export function getAppToken(){if(typeof window==='undefined')return '';return localStorage.getItem('nd-token')||''}
export async function api(path:string,init:RequestInit={}){const token=getAppToken();const headers=new Headers(init.headers||{});if(!(init.body instanceof FormData))headers.set('content-type','application/json');if(token)headers.set('authorization',`Bearer ${token}`);const res=await fetch(path,{...init,headers,cache:'no-store'});const body=await res.json().catch(()=>({}));if(res.status===401&&typeof window!=='undefined'){localStorage.removeItem('nd-token')}if(!res.ok)throw new Error(body.error||`Request failed (${res.status})`);return body}
