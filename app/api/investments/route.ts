import {NextRequest} from 'next/server';import {authContext,requirePermission} from '@/lib/auth';import {serviceSupabase} from '@/lib/supabase';import {fail,ok} from '@/lib/http';
export async function GET(req:NextRequest){try{const c=await authContext(req);requirePermission(c,'investments.view');const {data,error}=await serviceSupabase().from('investments').select('*').order('created_at',{ascending:false}).limit(100);if(error)throw error;return ok({ok:true,items:data||[]})}catch(e){return fail(e)}}
export async function POST(req:NextRequest){try{const c=await authContext(req);requirePermission(c,'investments.edit');const p=await req.json();const {data,error}=await serviceSupabase().rpc('nd_create_investment',{p_user_id:c.userId,p_payload:p});if(error)throw error;return ok(data,201)}catch(e){return fail(e)}}
